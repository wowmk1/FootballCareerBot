[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = [
  "rm -f .mise.toml .tool-versions .mise.config.toml .rtx.toml || true",
  "pip install --no-cache-dir -r requirements.txt"
]

[start]
cmd = "python bot.py"
